{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.2.10",
    "prettier": "3.4.1"
  },
  "scripts": {
    "prepare": "husky",
    "test": "npm --prefix backend run test && npm --prefix frontend run test"
  }
}
